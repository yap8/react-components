import Button from './components/Button';

const App = () => {
  return (
    <div>
      <section className="container">
        <h1>Components</h1>
      </section>
      <section className="container">
        <h2>Buttons</h2>
        <Button>Click me!</Button>
      </section>
    </div>
  );
};

export default App;
